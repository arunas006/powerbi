[project]
name = "powerbi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "python-dotenv",
    "pydantic>=2.12.0",
    "pydantic-settings>=2.8.0",
    "openai>=2.24.0",
    "numpy",
]
